# 2026-09-22

## ToDoList 应用（Vue 3）—— 规划阶段

- 用户目标：做一个能在浏览器里直接运行的 ToDoList，要求先给计划、暂不开发。
- 已确认选型（用户三选一的结果）：
  - 交付形态：单个 `index.html`，Vue 3 走 CDN 全局构建，双击即开，零 npm / 零构建
  - 数据存储：localStorage（key 拟定 `todolist.vue.v1`）
  - 功能范围：基础档（新增/编辑/删除/完成切换、三态筛选、剩余统计、清空已完成）
- 计划中的关键实现决策：
  - `createApp({ setup() })` + `reactive / computed / watch`，不走 Options API
  - `watch(todos, save, { deep: true })` 做持久化；读取时 try/catch + 结构校验兜底
  - 只用 `{{ }}` 插值，禁用 v-html；id 用 `crypto.randomUUID()` 降级方案
  - 中文输入法回车误触发：keydown 时判断 `e.isComposing`
  - 列表增删用 `<TransitionGroup>`
- 风险：file:// 下 Safari 的 localStorage 可能受限，兜底方案是本地起 `python -m http.server`。
- 状态：计划已交付，用户确认后进入开发。

## ToDoList 应用（Vue 3）—— 开发完成

- 产物：`D:/Ai-Works/WorkBuddy/test-dolist/index.html`，单文件 283 行，双击即开。
- 实现要点：
  - `createApp({ setup() })` + `toRefs(state)` 返回（**坑**：直接展开 reactive 会丢失响应性，必须 toRefs 或返回 state 整体）
  - `watch(() => state.todos, save, { deep: true })` 持久化；load 端 try/catch + 逐条结构校验
  - 存储不可用时 `storageOk=false`，页面顶部提示、降级为内存运行
  - 双击编辑：Enter 保存 / Esc 取消 / blur 保存 / 清空即删除；`e.isComposing` 防中文输入法误提交
  - `transition-group` 做列表增删动画（模板里必须用 kebab-case，in-DOM 模板大小写不敏感）
- 冒烟测试方案（可复用，无需装 Chromium）：
  - jsdom 装在 `C:/Users/Jacky/.workbuddy/binaries/node/workspace`，脚本 `todolist-smoke.mjs`
  - 把 CDN 的 `<script src>` 替换成本地 vue.global.prod.js 内联；**必须用函数式 replace**，否则 Vue 源码里的 `$&` 会被当成替换模式
  - jsdom 不触发 transitionend，离场元素会残留，断言要过滤掉 className 含 `leave` 的项
  - 按钮 `:disabled` 依赖异步 DOM 更新，模拟点击前要 `await tick()`
  - 结果：21/21 通过
- 备注：`agent-browser` 未安装（需下载 ~500MB Chromium），本机优先用 jsdom 无头方案。

## ToDoList 迭代二（4 项需求）

- 需求：tab 常驻显示 / 进行中 tab / 底部日历 / 导出功能。
- 说明：「进行中」tab 原本就存在，此前因 `footer` 带 `v-if="todos.length"` 导致无数据时整条底栏隐藏，用户看不到。
- 改动：
  - tab 从 footer 独立为常驻 `.tabs`，三个 tab 各带数量（全部 N / 进行中 N / 已完成 N）
  - 新增日历卡片（第二张 card）：月视图、周一起始、可翻月、当天有待办显示数量角标、点击日期筛选（再点取消），与三态 tab 叠加生效
  - 新增导出：Blob + `a[download]` 下载 `todolist-YYYY-MM-DD.json`，含 text/done/createdAt(ISO)
  - 日期键统一用 `dayKey(ts)` 生成 `YYYY-MM-DD`（本地时区），日历与筛选共用
- 冒烟脚本同步扩充到 36 项，全部通过。测试技巧：stub `window.Blob` 捕获导出内容、stub `HTMLAnchorElement.prototype.click` 只记录 download 名避免 jsdom 导航报错、过滤 jsdom 的 `Not implemented: navigation`。

## ToDoList 迭代三（项目结构拆分）

- 结构：
  ```
  index.html          入口模板 + Vue CDN
  css/style.css       样式
  js/app.js           逻辑
  README.md           启动命令与维护说明
  ```
- **关键约束**：`js/app.js` 必须是普通 `<script>`，**不能加 `type="module"`** —— ES module 在 `file://` 下会被 CORS 拦截，双击就打不开了。
- 启动命令：`python -m http.server 8000`（推荐）或 `npx serve .`；直接双击 index.html 也可行。已实测三者资源均 200。
- 冒烟脚本改为把 `js/app.js` 和 `css/style.css` 内联后再喂给 jsdom（jsdom 不加载外部本地子资源），仍是 36/36 通过。
- 注意：present_files 预览单 HTML 时只复制该文件，拆分结构下 css/js 会 404，需先起本地服务器并传 `http://localhost:8000`。

## ToDoList 迭代四（CSV 导入导出 + Logo）

- 导出改 CSV：`待办内容,状态,创建时间`，状态用 `是/否`，带 UTF-8 BOM（Excel 不乱码），文件名 `todolist-YYYY-MM-DD.csv`。
- 新增导入：`hidden` file input + `$refs.fileInput.click()` 触发；自写 `parseCsvLine` 处理引号包裹与 `""` 转义；跳过表头、空行；状态识别 `是/true/1/yes/已完成`；时间解析失败降级为今天。
- **踩坑**：导入按钮原本在 `footer`（`v-if="todos.length"`）里，无数据时整条隐藏 → 首次打开无法导入。改为 footer 常驻：统计与「清空已完成」条件显示，「导入」常驻、「导出」无数据时 disabled。
- Logo：内联 SVG（`.brand` 内 34px）+ 同款 data URI favicon，蓝底 `#3b6fd4` 圆角方块 + 白色对勾，无图片依赖。
- 冒烟扩到 44 项全通过。jsdom 测试技巧补充：`window.alert` 要 stub；用 `Object.defineProperty(input,'files',{value:[file]})` 注入 `window.File`；FileReader 异步需 tick 300。

## ToDoList 迭代五（Logo 重设计，用 frontend-design skill）

- 用户反馈旧 logo（纯蓝方块 + 白勾）不好看。新设计：**蓝色渐变圆角方块 + 左侧白色清单卡片（三条长短不一的待办行）+ 右侧白色对勾**，语义「待办 → 完成」，比通用勾有辨识度。
- 细节：渐变 `#4d7ee0 → #2f5cb4`；清单第三条最短暗示未填满；勾用 `stroke-dasharray` 做加载时一次性描边动画，并支持 `prefers-reduced-motion`。
- 标题做分色：`ToDo`(深色) + `List`(主色 #3b6fd4)，用 `.title-accent`。
- favicon 用同款 data URI SVG；**data URI 里所有 `#` 必须写成 `%23`**（含 `url(#g)` → `url(%23g)`），否则被当 fragment 截断。
- 冒烟 46/46 通过。

## ToDoList 接入 MySQL —— 选型确定（未开工）

- 环境实测：本机 MySQL **8.0.46** 已在 127.0.0.1:3306 运行，root/123456 可登录，仅有系统库，`todolist` 库待建。
  （此前探测误判为"无 MySQL"，因为 mysqld 不在 PATH、服务名不含 mysql，实际是免安装版在跑 —— 判断数据库是否存在要直接连端口，别只看 PATH/服务列表。）
- 用户拍板的选型：本地 MySQL 8 + **Python FastAPI** 后端 + **多用户（登录注册）**。
- 依赖选型（避坑）：`bcrypt` 直接用，**不要用 passlib**（与 bcrypt 4.x 有已知兼容问题）；JWT 用 `PyJWT` 而非 python-jose。
- 密码哈希存 `CHAR(60)`；JWT 有效期 7 天，走 `Authorization: Bearer`；所有 todo 查询强制带 `WHERE user_id = ?` 做隔离。
- Python venv 已建：`C:/Users/Jacky/.workbuddy/binaries/python/envs/default`，已装 pymysql。
- 状态：计划已交付，等待用户确认后开工。

## ToDoList 迭代六（MySQL 接入落地：登录页 + bcrypt + 10 分钟令牌）

- 用户补充要求：常用加密（bcrypt）、**令牌有效期 10 分钟**、**保留本地存储兜底**、要有独立登录页。
- 后端 `server/`（FastAPI）：`main.py` 路由 + `auth.py`(bcrypt/JWT) + `db.py`(pymysql utf8mb4) + `schema.sql` + `.env`。
  - 依赖实测版本：fastapi 0.141、PyJWT 2.14、**bcrypt 5.0**（`hashpw/gensalt/checkpw` 用法不变，无需 passlib）。
  - **坑**：`JSONResponse(content, status_code)` —— 我把参数写反成 `(status_code, content)`，Starlette 报 `TypeError: '<' not supported between dict and int`。响应构造参数顺序别想当然。
  - 统一用 `@app.exception_handler(HTTPException)` 把错误转成 `{ok:false,error}`，前端只判一种结构。
  - 越权与不存在都返回 404（不暴露资源是否存在）；10 分钟令牌太短，加了 `/api/auth/refresh` 做滑动续期。
- 前端新增 `js/api.js`（全局 `window.API`，非 module）：fetch 封装、Bearer 头、401 自动清会话、按 JWT `exp` 提前 1 分钟 `setTimeout` 续期、AbortController 超时 8s。
  - 续期要点：前端自己 `atob` 解 JWT 第二段拿 `exp`（mock 令牌也必须是 `header.payload.signature` 三段结构，否则前端识别不了过期）。
- 模式切换（`app.js`）：启动时 `API.health()` 探测 → 后端不可达或用户点「本地模式」→ `mode='local'` 走 localStorage；否则在线模式，未登录显示登录页。
  - 在线模式下 localStorage 仍写缓存 `todolist.cache.<userId>`，仅兜底展示。
  - 所有写操作乐观更新 + 失败回滚并顶部提示；复选框从 `v-model` 改成 `:checked + @change`（在线要发 PATCH）。
- 登录页：`.auth` 卡片，登录/注册切换、用户名密码框、错误提示、「不登录，用本地模式」入口；顶栏显示用户名 + 退出。
- 测试：
  - 后端 `todolist-api-test.py`（urllib）16/16：越权 404、无效令牌 401、刷新令牌、清空已完成等。
  - jsdom 冒烟重写为 5 个阶段（后端 down 回归 / 登录注册同步 / 带令牌重开 / 令牌过期 / 手动本地模式），**83/83**。关键技巧：用 `JSDOM` 的 `beforeParse` 注入 `window.fetch` mock（脚本在构造时就执行，之后注入来不及）；mock 后端要在**多个 jsdom 实例间共享**，否则数据不连续。
  - `todolist-e2e-real.mjs` 直连真实 FastAPI+MySQL 全栈 9/9：jsdom 里 `window.fetch = (u,o)=>{const {signal,...r}=o; return fetch(u,r)}`（jsdom 的 AbortSignal 不能给 Node fetch 用）。
  - 断言 localStorage 清除要用 `!getItem(k)`，`removeItem` 后是 `null` 不是 `''`。
- 数据库测试数据已清理（users 表清空，外键级联删除 todos）。

## ToDoList 迭代七（登录页密码框样式修复）

- 现象：登录页密码框外观与用户名框不一致（高度、圆角、边框、字体都不同）。
- 根因：CSS 里输入框样式只写了 `input[type="text"], .edit-input`，**漏了 `input[type="password"]`**，密码框走浏览器默认样式；登录页是项目里第一个 password 框，之前没暴露。
- 修复：把 `input[type="password"]` 加进基础样式与 `:focus` 选择器组。
- 教训：新增一种 input 类型时，要回查通用输入样式选择器是否覆盖它；可用 jsdom `getComputedStyle` 对比两个框的 padding/radius/border/font 做回归断言（脚本 `style-check.mjs`）。
- 回归：冒烟 83/83 仍通过。

## ToDoList 迭代八（分页 + 开始结束时间 + 提醒弹窗）

- 需求：每页 5 条分页；新增时可填开始/结束时间（改 todos 表）；可设开始时间前 5/10/15/20/25/30 分钟提醒，右下角弹窗。
- 数据库：`todos` 加 `start_at DATETIME(3) / end_at DATETIME(3) / remind_minutes SMALLINT UNSIGNED` 三列，可空。
  - 已有旧库用 `server/migrate_times.py` 幂等升级（先查 `information_schema.COLUMNS` 再 ALTER，避免 `CREATE TABLE IF NOT EXISTS` 不加列的问题）。
- 后端：时间统一毫秒时间戳（0/None 表示未设置），`DATETIME(3)` 存取；校验三条——结束不能早于开始、档位只能是 5/10/15/20/25/30、设提醒必须有开始时间。
  - PATCH 用 `model_dump(exclude_unset=True)` 做局部更新；时间字段要**先与库中现有值合并再整体校验**，否则只改一半会造出倒挂区间。
- 前端：
  - 分页 `pagedTodos = filteredTodos.slice((p-1)*5, p*5)`；切换筛选重置 page，`watch(totalPages)` 把越界页码夹回；新增后跳到末页以便看到刚加的项。
  - **坑**：`filteredTodos` 在「全部」筛选下原本直接 `return state.todos`，原地 push 后**引用不变 → 依赖它的 watch 不触发**，导致提醒检查漏跑。改成始终 `return list.slice()`。
  - 提醒：`setInterval(checkReminders, 30000)` + 列表变化时触发；`notified` Set 去重；触发窗口 `[startAt - remind*60000, +1小时]`，避免隔天打开被过期任务刷屏；已完成/未设提醒的不弹。
  - 列表项改为 `.item-main` 包裹（文本 + `.item-meta` 时间行），复选框 `align-items: flex-start` 对齐。
- 测试：
  - 后端 `todolist-api-times.py` 19/19；jsdom 冒烟 83 → **127/127**（新增阶段 F 分页 / G 时间+弹窗 / H 提醒过滤）。
  - 真全栈 `todolist-e2e-real.mjs` 扩到 18/18，直接查 `/api/todos` 校验 MySQL 里的 startAt/endAt/remindMinutes。
  - **测试坑**：新增失败时表单保留已填内容（方便用户修正），所以测试里再次新增必须**显式清空** datetime-local，否则残留值会让下一次新增被别的规则拦住、断言看起来"提示没更新"。
  - jsdom 支持 `datetime-local`：赋值 `YYYY-MM-DDTHH:mm` 可正常读取，`Date.parse` 也能解析。

## ToDoList 迭代九（快捷编辑面板 + 删除撤销）

- 用户选择：快捷编辑 = **行内展开面板**；删除 = **图标按钮 + 撤销条**（不是二次确认）。
- 快捷编辑：每条右侧铅笔按钮 → 该行原地展开 `.quick-edit`（文本 + 开始/结束时间 + 提醒 + 保存/取消），回车保存 / Esc 取消。
  - 与双击编辑（`editingId`）并存但互斥：`openQuickEdit` 会先清 `editingId`；`quickEditId` 是单值，天然保证同时只展开一条。
  - 保存走 `API.updateTodo(id, {text,startAt,endAt,remindMinutes})`，乐观更新 + 失败 `Object.assign(todo, prev)` 回滚。
  - 面板里清空文本是**拦截并提示**（不再沿用双击编辑的"清空即删除"，因为有独立删除按钮了）。
- 删除：`removeTodo` 改为 `splice` 并记录 `{todo, index}` 到 `state.undo`，8 秒 `setTimeout` 清除；撤销时 `splice(min(index,len),0,todo)` 插回原位。
  - 在线模式撤销要重新 `API.createTodo`（后端那条已真删），失败则移除并提示。
  - 按钮从 `×` 文字改成内联 SVG（铅笔/垃圾桶），30px 圆形图标按钮，hover 分别蓝/红底；class `del` 保留以便测试选择器不变。
- **可访问性坑**：按钮默认 `opacity:0` 靠 hover 显现，触屏设备（含宽屏平板）没有 hover 会完全点不到 → 加 `@media (hover: none) { .ops button { opacity: 1 } }`。
- 撤销条固定底部居中，同时出现提醒弹窗会重叠 → 给 toasts 容器加 `:class="{'with-undo': !!undo}"` 上移 78px。
- 冒烟 127 → **164/164**（新增阶段 I 快捷编辑、阶段 J 在线删除/撤销同步后端）；真实全栈 18/18 无回归。

## 迭代十（提交到 GitHub）

- 仓库：https://github.com/benniao0811/ai-dolist.git ，本地 `git init` 后首个提交 `7788828`（14 文件 / 2167 行），分支 `main`。
- 敏感文件已隔离：`.gitignore` 含 `server/.env`，只提交 `.env.example`（数据库密码不入库）。提交前务必确认 `.env` 未被 `git add`。
- **网络坑**：沙箱代理拒绝 `github.com:443` 的 CONNECT（502），HTTPS push 走不通；`github.com:22` 可达 → remote 改用 `git@github.com:benniao0811/ai-dolist.git`，并生成 `~/.ssh/id_ed25519`。
- 状态：**已推送成功**（`main -> main`，14 个文件）。用户把 ed25519 公钥加进 GitHub 后 `git push -u origin main` 一次通过。
- 推送时有 warning：`hostfile_replace_entries ... known_hosts: Permission denied`（沙箱不允许改写 known_hosts），不影响推送结果，可忽略。
- 后续协作：远程默认分支 `main`，用 SSH remote。敏感文件检查命令：`git ls-tree -r --name-only origin/main | grep '\.env$'`。
